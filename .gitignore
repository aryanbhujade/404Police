# Catalog health-check output. Generated locally and in CI, never committed:
# it is a point-in-time snapshot of third-party uptime, not source.
reports/

# Editor / OS noise
.DS_Store
.idea/
.vscode/

# There are no runtime dependencies; if one ever appears, it stays out of the
# deployed root — GitHub Pages publishes this tree verbatim.
node_modules/

# Original server-side test kept locally while this branch serves the static build.
/app.test.js
